services:
  app:
    build: .
    ports:
      - "4059:80"
    restart: unless-stopped
    environment:
      - DATABASE_URL=postgresql://student_admin:vibecode2026pg@172.17.0.1:5433/db_olgak
      - JWT_SECRET=nmt2026_super_secret_jwt_key_olgak
      - PORT=80
